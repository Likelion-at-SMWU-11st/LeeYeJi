import React from 'react';
import '../src/App.css'

function Title ({ title, name }) {
    return (
        <div class="body">
            <h1>{title}</h1>
        </div>
    );
};

export default Title;